namespace Severina.Domain.Enums;

public enum StatusCliente
{
    Ativo = 0,
    Inativo = 1,
    Pendente = 2
}

public enum InteractionType
{
    Message = 0,
    Call = 1,
    Email = 2,
    Note = 3,
    Appointment = 4
}
